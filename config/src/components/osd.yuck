
(defwidget osd-container [visible icon-text name]
  (box
    :visible {visible}
    :space-evenly false
    :spacing 10
    :class "osd-container panel"
    (icon :name {icon-text})
    (label :text {name})
  )
)
