(defwidget left-bar []
  (box
    :valign "center"
    :halign "start"
    :space-evenly false
    :spacing 0
    :class "left-bar"

    (box
      :class "bb-wrap l"
      (button
        :class "btn-bar"
        (vector :name "nixos")
      )
    )

    (box
      :class "bb-wrap l"
      (button
        :class "btn-bar"
        (icon :name "search")
      )
    )

    (box
      :class "bb-wrap l"
      (button
        :class "btn-bar"
        (icon :name "activity")
      )
    )
   
    (box
      :class "bb-wrap l"
      (button
        :class "btn-bar"
        :onclick "hyprctl keyword opacity 0"
        (label 
          :limit-width 20
          :text {ACTIVE_WINDOW.title=="null" ? "Desktop" : ACTIVE_WINDOW.title}
        )
      )
    )
  )
)
